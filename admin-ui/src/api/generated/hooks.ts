// ============================================================
// AUTO-GENERATED - DO NOT EDIT
// Generated by: npm run generate-api-client
// ============================================================

import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { ArcheClient } from './client';
import type {
  AuditLogListQuery,
  BatchAssignRequest,
  BatchDeleteRequest,
  BatchEditRequest,
  BlueprintListQuery,
  ConflictResolutionRequest,
  CreateApiKeyRequest,
  CreateAffixRequest,
  CreateBlueprintRequest,
  CreateClientRequest,
  CreateGlobalMetaAttributeRequest,
  ExportRequest,
  GenerateRequest,
  UpdateAffixRequest,
  UpdateBlueprintRequest,
  UpdateGlobalMetaAttributeRequest,
} from './types';

let _client: ArcheClient | null = null;

export function getClient(): ArcheClient {
  if (!_client) {
    throw new Error('ArcheClient not initialized. Call setClient() first or pass client to hooks.');
  }
  return _client;
}

export function setClient(client: ArcheClient): void {
  _client = client;
}

export function useArcheClient(client?: ArcheClient): ArcheClient {
  if (client) return client;
  return getClient();
}

// --- Generate ---

export function useGenerate(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: GenerateRequest) => c.generate(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['generate'] });
    },
  });
}

// --- Blueprints ---

export function useBlueprintsList(query?: BlueprintListQuery, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['blueprints', 'list', query],
    queryFn: () => c.listBlueprints(query),
  });
}

export function useBlueprint(id: string, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['blueprints', 'detail', id],
    queryFn: () => c.getBlueprint(id),
    enabled: !!id,
  });
}

export function useCreateBlueprint(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: CreateBlueprintRequest) => c.createBlueprint(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
    },
  });
}

export function useUpdateBlueprint(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, request }: { id: string; request: UpdateBlueprintRequest }) =>
      c.updateBlueprint(id, request),
    onSuccess: (_data, variables) => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
      queryClient.invalidateQueries({ queryKey: ['blueprints', 'detail', variables.id] });
    },
  });
}

export function useDeleteBlueprint(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => c.deleteBlueprint(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
    },
  });
}

// --- Batch: Blueprints ---

export function useBatchDeleteBlueprints(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: BatchDeleteRequest) => c.batchDeleteBlueprints(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
    },
  });
}

export function useBatchAssignBlueprints(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: BatchAssignRequest) => c.batchAssignBlueprints(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
    },
  });
}

export function useBatchEditBlueprints(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: BatchEditRequest) => c.batchEditBlueprints(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
    },
  });
}

// --- Affixes ---

export function useAffixesList(
  query?: { cursor?: string; limit?: number; page?: number; perPage?: number; search?: string },
  client?: ArcheClient,
) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['affixes', 'list', query],
    queryFn: () => c.listAffixes(query),
  });
}

export function useAffix(id: string, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['affixes', 'detail', id],
    queryFn: () => c.getAffix(id),
    enabled: !!id,
  });
}

export function useCreateAffix(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: CreateAffixRequest) => c.createAffix(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
    },
  });
}

export function useUpdateAffix(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, request }: { id: string; request: UpdateAffixRequest }) =>
      c.updateAffix(id, request),
    onSuccess: (_data, variables) => {
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
      queryClient.invalidateQueries({ queryKey: ['affixes', 'detail', variables.id] });
    },
  });
}

export function useDeleteAffix(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => c.deleteAffix(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
    },
  });
}

// --- Batch: Affixes ---

export function useBatchDeleteAffixes(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: BatchDeleteRequest) => c.batchDeleteAffixes(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
    },
  });
}

export function useBatchAssignAffixes(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: BatchAssignRequest) => c.batchAssignAffixes(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
    },
  });
}

// --- Global Meta Attributes ---

export function useGlobalMetaAttributesList(
  query?: { cursor?: string; limit?: number; page?: number; perPage?: number; search?: string },
  client?: ArcheClient,
) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['globalMetaAttributes', 'list', query],
    queryFn: () => c.listGlobalMetaAttributes(query),
  });
}

export function useGlobalMetaAttribute(id: string, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['globalMetaAttributes', 'detail', id],
    queryFn: () => c.getGlobalMetaAttribute(id),
    enabled: !!id,
  });
}

export function useCreateGlobalMetaAttribute(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: CreateGlobalMetaAttributeRequest) =>
      c.createGlobalMetaAttribute(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['globalMetaAttributes'] });
    },
  });
}

export function useUpdateGlobalMetaAttribute(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, request }: { id: string; request: UpdateGlobalMetaAttributeRequest }) =>
      c.updateGlobalMetaAttribute(id, request),
    onSuccess: (_data, variables) => {
      queryClient.invalidateQueries({ queryKey: ['globalMetaAttributes'] });
      queryClient.invalidateQueries({ queryKey: ['globalMetaAttributes', 'detail', variables.id] });
    },
  });
}

export function useDeleteGlobalMetaAttribute(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => c.deleteGlobalMetaAttribute(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['globalMetaAttributes'] });
    },
  });
}

// --- Clients ---

export function useClientsList(
  query?: { cursor?: string; limit?: number; page?: number; perPage?: number },
  client?: ArcheClient,
) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['clients', 'list', query],
    queryFn: () => c.listClients(query),
  });
}

export function useClient(id: string, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['clients', 'detail', id],
    queryFn: () => c.getClient(id),
    enabled: !!id,
  });
}

export function useCreateClient(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: CreateClientRequest) => c.createClient(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['clients'] });
    },
  });
}

export function useDeleteClient(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => c.deleteClient(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['clients'] });
    },
  });
}

// --- API Keys ---

export function useApiKeys(clientId: string, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['clients', 'keys', clientId],
    queryFn: () => c.listApiKeys(clientId),
    enabled: !!clientId,
  });
}

export function useCreateApiKey(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ clientId, request }: { clientId: string; request: CreateApiKeyRequest }) =>
      c.createApiKey(clientId, request),
    onSuccess: (_data, variables) => {
      queryClient.invalidateQueries({ queryKey: ['clients'] });
      queryClient.invalidateQueries({ queryKey: ['clients', 'keys', variables.clientId] });
    },
  });
}

export function useDeleteApiKey(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ clientId, keyId }: { clientId: string; keyId: string }) =>
      c.deleteApiKey(clientId, keyId),
    onSuccess: (_data, variables) => {
      queryClient.invalidateQueries({ queryKey: ['clients', 'keys', variables.clientId] });
    },
  });
}

// --- Audit Log ---

export function useAuditLog(query?: AuditLogListQuery, client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['auditLog', query],
    queryFn: () => c.listAuditLog(query),
  });
}

// --- Export / Import ---

export function useExportData(client?: ArcheClient) {
  const c = useArcheClient(client);
  return useMutation({
    mutationFn: (request: ExportRequest) => c.exportData(request),
  });
}

export function useImportData(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (formData: FormData) => c.importData(formData),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
      queryClient.invalidateQueries({ queryKey: ['clients'] });
    },
  });
}

export function useResolveImport(client?: ArcheClient) {
  const c = useArcheClient(client);
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (request: ConflictResolutionRequest) => c.resolveImport(request),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['blueprints'] });
      queryClient.invalidateQueries({ queryKey: ['affixes'] });
      queryClient.invalidateQueries({ queryKey: ['clients'] });
    },
  });
}

// --- Schemas ---

export function useBlueprintsSchema(client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['schema', 'blueprints'],
    queryFn: () => c.getBlueprintsSchema(),
  });
}

export function useAffixesSchema(client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['schema', 'affixes'],
    queryFn: () => c.getAffixesSchema(),
  });
}

export function useGenerateSchema(client?: ArcheClient) {
  const c = useArcheClient(client);
  return useQuery({
    queryKey: ['schema', 'generate'],
    queryFn: () => c.getGenerateSchema(),
  });
}