/**
 * User-facing mount configuration for bind-mount sandbox providers.
 *
 * Each entry describes a host directory to mount into the sandbox container.
 */
export {};
//# sourceMappingURL=MountConfig.js.map